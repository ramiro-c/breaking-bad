import React from "react";
import logo from "../assets/images/logo.svg";

const Logo = () => {
  return (
    <img
      style={{  maxWidth: "85vw" }}
      src={logo}
      alt="breaking bad logo"
    />
  );
};

export default Logo;
